######## LEGACY STYLE BLOCK: public/admin/agencies.html ########
  <style>
    .admin-badge { display:inline-block; background:#FF0000; color:white; font-size:.65rem; font-weight:700; letter-spacing:.06em; text-transform:uppercase; padding:2px 7px; border-radius:4px; vertical-align:middle; margin-left:6px; }
    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:660px; max-height:90vh; overflow-y:auto; padding:32px; box-shadow:var(--shadow-lg); }
    .modal-title { font-weight:700; font-size:1.1rem; margin-bottom:4px; }
    .section-heading { font-weight:600; font-size:.9rem; color:var(--charcoal); margin-bottom:12px; }
    .section-divider { border:none; border-top:1px solid var(--gray-200); margin:20px 0; }
    tr.clickable { cursor:pointer; }
    tr.clickable:hover td { background:var(--gray-100); }
    .agent-row { display:flex; align-items:center; gap:10px; padding:10px 0; border-bottom:1px solid var(--gray-100); }
    .agent-row:last-child { border-bottom:none; }
    .toggle-verified { display:inline-flex; align-items:center; gap:6px; padding:5px 12px; border-radius:20px; font-size:.8rem; font-weight:600; cursor:pointer; border:1.5px solid; transition:.15s; }
    .toggle-verified.verified   { background:#F0FDF4; color:#15803D; border-color:#86EFAC; }
    .toggle-verified.unverified { background:#FEF2F2; color:#B91C1C; border-color:#FECACA; }
  </style>

######## LEGACY STYLE BLOCK: public/admin/certificates.html ########
  <style>
    .admin-badge { display:inline-block; background:#FF0000; color:white; font-size:.65rem; font-weight:700; letter-spacing:.06em; text-transform:uppercase; padding:2px 7px; border-radius:4px; vertical-align:middle; margin-left:6px; }
    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:500px; padding:32px; box-shadow:var(--shadow-lg); }
    .modal-title { font-weight:700; font-size:1.1rem; margin-bottom:4px; }
    .modal-sub { font-size:.875rem; color:var(--gray-400); margin-bottom:20px; }
    .deactivated td { opacity:.45; }
  </style>

######## LEGACY STYLE BLOCK: public/admin/dashboard.html ########
  <style>
    .admin-badge { display:inline-block; background:#FF0000; color:white; font-size:.65rem; font-weight:700; letter-spacing:.06em; text-transform:uppercase; padding:2px 7px; border-radius:4px; vertical-align:middle; margin-left:6px; }
    .activity-row { display:flex; align-items:center; gap:10px; padding:9px 0; border-bottom:1px solid var(--gray-100); font-size:.8125rem; }
    .activity-row:last-child { border-bottom:none; }
    .activity-icon { width:28px; height:28px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:.85rem; flex-shrink:0; }

    .usage-tab-bar { display:flex; gap:0; border-bottom:2px solid var(--gray-200); margin-bottom:20px; }
    .usage-tab-btn { padding:8px 18px; border:none; background:none; font-size:.825rem; font-weight:600; color:var(--gray-400); cursor:pointer; border-bottom:2px solid transparent; margin-bottom:-2px; transition:color .15s,border-color .15s; }
    .usage-tab-btn.active { color:var(--charcoal); border-bottom-color:var(--charcoal); }

    .usage-member-row { display:flex; align-items:center; gap:12px; padding:8px 0; border-bottom:1px solid var(--gray-100); }
    .usage-member-row:last-child { border-bottom:none; }
    .usage-member-name { font-size:.8125rem; font-weight:600; width:180px; flex-shrink:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
    .usage-bar-wrap { flex:1; background:var(--gray-200); border-radius:9999px; height:7px; overflow:hidden; }
    .usage-bar-fill { height:100%; border-radius:9999px; background:var(--charcoal); transition:width .4s ease; }
    .usage-bar-fill.warn { background:var(--amber); }
    .usage-bar-fill.over { background:var(--red); }
    .usage-count-col { font-size:.8rem; font-weight:600; width:48px; text-align:right; flex-shrink:0; color:var(--charcoal); }
    .usage-limit-lbl { font-size:.75rem; color:var(--gray-400); width:52px; text-align:left; flex-shrink:0; }
  </style>

######## LEGACY STYLE BLOCK: public/admin/documents.html ########
  <style>
    .admin-badge { display:inline-block; background:#FF0000; color:white; font-size:.65rem; font-weight:700; letter-spacing:.06em; text-transform:uppercase; padding:2px 7px; border-radius:4px; vertical-align:middle; margin-left:6px; }
    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.5); z-index:200; align-items:flex-start; justify-content:center; padding:40px 16px; overflow-y:auto; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:680px; padding:32px; box-shadow:var(--shadow-lg); }
    .modal-title { font-weight:700; font-size:1.1rem; margin-bottom:4px; }
    .modal-sub { font-size:.875rem; color:var(--gray-400); margin-bottom:20px; }
    .ocr-field-row { display:grid; grid-template-columns:160px 1fr; gap:8px; align-items:center; margin-bottom:8px; }
    .ocr-field-key { font-size:.8125rem; font-weight:600; color:var(--gray-500); }
    .ocr-field-val input { padding:6px 10px; font-size:.8125rem; width:100%; border:1px solid var(--gray-200); border-radius:var(--radius); font-family:var(--font); }
    .ocr-field-val input:focus { outline:none; border-color:var(--charcoal); }
    .conf-bar { height:6px; background:var(--gray-200); border-radius:3px; margin-top:4px; overflow:hidden; }
    .conf-bar__fill { height:100%; border-radius:3px; }
    .tab-bar { display:flex; gap:0; border-bottom:2px solid var(--gray-200); margin-bottom:20px; }
    .tab { padding:8px 16px; font-size:.875rem; font-weight:500; color:var(--gray-400); cursor:pointer; border-bottom:2px solid transparent; margin-bottom:-2px; }
    .tab.active { color:var(--charcoal); border-bottom-color:var(--charcoal); font-weight:700; }
    .status-select { padding:6px 10px; border:1px solid var(--gray-200); border-radius:var(--radius); font-size:.8125rem; font-family:var(--font); }
  </style>

######## LEGACY STYLE BLOCK: public/admin/owners.html ########
  <style>
    .admin-badge { display:inline-block; background:#FF0000; color:white; font-size:.65rem; font-weight:700; letter-spacing:.06em; text-transform:uppercase; padding:2px 7px; border-radius:4px; vertical-align:middle; margin-left:6px; }
    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:480px; padding:32px; box-shadow:var(--shadow-lg); }
    .modal-title { font-weight:700; font-size:1.1rem; margin-bottom:20px; }
  </style>

######## LEGACY STYLE BLOCK: public/admin/tenants.html ########
  <style>
    .admin-badge { display:inline-block; background:#FF0000; color:white; font-size:.65rem; font-weight:700; letter-spacing:.06em; text-transform:uppercase; padding:2px 7px; border-radius:4px; vertical-align:middle; margin-left:6px; }
    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:720px; max-height:90vh; overflow-y:auto; padding:32px; box-shadow:var(--shadow-lg); }
    .modal-title { font-weight:700; font-size:1.1rem; margin-bottom:4px; }
    .field-grid { display:grid; grid-template-columns:1fr 1fr; gap:12px; }
    .section-heading { font-weight:600; font-size:.9rem; color:var(--charcoal); margin-bottom:12px; }
    .section-divider { border:none; border-top:1px solid var(--gray-200); margin:20px 0; }
    .doc-row { display:flex; align-items:center; gap:10px; padding:8px 0; border-bottom:1px solid var(--gray-100); }
    .doc-row:last-child { border-bottom:none; }
    .search-bar { display:flex; gap:10px; margin-bottom:20px; }
    .search-bar input { flex:1; }
  </style>

######## LEGACY STYLE BLOCK: public/agency/dashboard.html ########
  <style>
    .tab-bar { display:flex; gap:0; border-bottom:2px solid var(--gray-200); margin-bottom:28px; overflow-x:auto; }
    .tab-btn { padding:10px 22px; border:none; background:none; font-size:.9rem; font-weight:600; color:var(--gray-400); cursor:pointer; border-bottom:2px solid transparent; margin-bottom:-2px; transition:color .15s,border-color .15s; white-space:nowrap; flex-shrink:0; }
    .tab-btn.active { color:var(--charcoal); border-bottom-color:var(--charcoal); }
    .tab-btn:hover:not(.active) { color:var(--charcoal); }

    .invite-grid { display:grid; grid-template-columns:1fr 1fr; gap:16px; margin-bottom:24px; }
    @media (max-width:720px) { .invite-grid { grid-template-columns:1fr; } }

    .filter-bar { display:flex; gap:8px; flex-wrap:wrap; align-items:center; margin-bottom:20px; }
    .filter-bar select, .filter-bar input[type="text"] { width:auto; padding:8px 12px; font-size:.875rem; }
    .filter-bar label { display:flex; align-items:center; gap:6px; font-size:.875rem; cursor:pointer; white-space:nowrap; }

    .addr-group { border:1px solid var(--gray-200); border-radius:var(--radius); margin-bottom:12px; overflow:hidden; }
    .addr-group-header { display:flex; align-items:center; justify-content:space-between; padding:14px 16px; background:var(--gray-100); cursor:pointer; gap:12px; user-select:none; }
    .addr-group-header:hover { background:var(--gray-200); }
    .addr-group-title { font-weight:600; font-size:.9375rem; }
    .addr-group-meta { font-size:.8125rem; color:var(--gray-400); margin-top:2px; }
    .addr-group-toggle { color:var(--gray-400); font-size:.875rem; flex-shrink:0; }
    .addr-group-body { display:none; }
    .addr-group-body.open { display:block; }
    .addr-group-body table { margin:0; border-radius:0; }
    .addr-group-body table td, .addr-group-body table th { padding:12px 16px; }

    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; padding:16px; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:520px; padding:32px; box-shadow:var(--shadow-lg); max-height:90vh; overflow-y:auto; }
    .modal-title { font-size:1.1rem; font-weight:700; margin-bottom:4px; }
    .modal-subtitle { font-size:.875rem; color:var(--gray-400); margin-bottom:24px; }

    .kpi-grid-4 { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin-bottom:32px; }
    @media (max-width:900px) { .kpi-grid-4 { grid-template-columns:repeat(2,1fr); } }
    @media (max-width:500px) { .kpi-grid-4 { grid-template-columns:1fr 1fr; } }

    .kpi-two-col { display:grid; grid-template-columns:1fr 1fr; gap:20px; margin-bottom:24px; }
    @media (max-width:680px) { .kpi-two-col { grid-template-columns:1fr; } }

    .mini-stat-row { display:flex; gap:12px; margin-bottom:20px; }
    .mini-stat { flex:1; background:var(--gray-100); border-radius:8px; padding:14px 16px; }
    .mini-stat-num { font-size:1.5rem; font-weight:700; }
    .mini-stat-lbl { font-size:.75rem; color:var(--gray-400); margin-top:2px; }

    .dist-bar { display:flex; align-items:center; gap:12px; margin-bottom:10px; }
    .dist-bar-label { font-size:.8125rem; width:80px; flex-shrink:0; color:var(--gray-500); }
    .dist-bar-track { flex:1; background:var(--gray-200); border-radius:9999px; height:8px; overflow:hidden; }
    .dist-bar-fill { height:100%; border-radius:9999px; background:var(--charcoal); transition:width .4s; }
    .dist-bar-count { font-size:.8125rem; font-weight:600; width:24px; text-align:right; flex-shrink:0; }
  </style>

######## LEGACY STYLE BLOCK: public/agency/dossier.html ########
  <style>
    .preview-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.75); z-index:300; align-items:center; justify-content:center; padding:24px; }
    .preview-overlay.open { display:flex; }
    .preview-box { background:#fff; border-radius:var(--radius); width:100%; max-width:920px; display:flex; flex-direction:column; box-shadow:0 20px 60px rgba(0,0,0,.3); max-height:90vh; }
    .preview-header { display:flex; align-items:center; justify-content:space-between; padding:14px 20px; border-bottom:1px solid var(--gray-200); flex-shrink:0; gap:12px; }
    .preview-title { font-weight:600; font-size:.9375rem; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; min-width:0; }
    .preview-close { background:none; border:none; cursor:pointer; font-size:1.1rem; color:var(--gray-400); padding:4px 10px; border-radius:4px; line-height:1; flex-shrink:0; }
    .preview-close:hover { background:var(--gray-100); color:var(--charcoal); }
    .preview-body { flex:1; overflow:auto; min-height:0; background:var(--gray-100); }
    .preview-body iframe { width:100%; height:75vh; min-height:400px; border:none; display:block; }
    .preview-body img { max-width:100%; max-height:75vh; display:block; margin:auto; padding:16px; box-sizing:border-box; }
    .preview-loading { display:flex; align-items:center; justify-content:center; height:300px; font-size:.875rem; color:var(--gray-400); }
    .preview-err { display:flex; align-items:center; justify-content:center; height:200px; font-size:.875rem; color:var(--red); }
  </style>

######## LEGACY STYLE BLOCK: public/agency/settings.html ########
  <style>
    .tab-bar { display:flex; gap:0; border-bottom:2px solid var(--gray-200); margin-bottom:28px; }
    .tab-btn { padding:10px 22px; border:none; background:none; font-size:.9rem; font-weight:600; color:var(--gray-400); cursor:pointer; border-bottom:2px solid transparent; margin-bottom:-2px; transition:color .15s, border-color .15s; }
    .tab-btn.active { color:var(--charcoal); border-bottom-color:var(--charcoal); }

    .danger-zone { border-color: #FECACA !important; }
    .danger-zone legend { color: var(--red); }

    .payment-method-card { border:1px solid var(--gray-200); border-radius:var(--radius); padding:16px 20px; display:flex; align-items:center; gap:14px; margin-bottom:12px; }
    .payment-method-card.is-default { border-color:#86EFAC; background:#F0FDF4; }
    .payment-method-icon { font-size:1.5rem; flex-shrink:0; width:36px; text-align:center; }
    .payment-method-info { flex:1; min-width:0; }
    .payment-method-name { font-weight:600; font-size:.9rem; }
    .payment-method-sub { font-size:.8rem; color:var(--gray-400); margin-top:2px; }

    .invoice-row { display:flex; align-items:center; gap:12px; padding:12px 0; border-bottom:1px solid var(--gray-100); }
    .invoice-row:last-child { border-bottom:none; }

    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:500px; padding:32px; box-shadow:var(--shadow-lg); max-height:90vh; overflow-y:auto; }

    .phone-row { display:flex; gap:8px; }
    .phone-row select { width:110px; flex-shrink:0; }
    .phone-row input { flex:1; }

    .tax-id-list { display:flex; flex-direction:column; gap:8px; margin-bottom:8px; }
    .tax-id-row { display:flex; gap:8px; }
    .tax-id-row input { flex:1; }

    .plan-banner { display:flex; align-items:center; justify-content:space-between; gap:16px; padding:16px 20px; background:var(--gray-100); border-radius:var(--radius); margin-bottom:24px; flex-wrap:wrap; }
    .plan-name { font-weight:700; font-size:1rem; }
    .plan-meta { font-size:.8rem; color:var(--gray-400); margin-top:2px; }
    .usage-item { margin-bottom:20px; }
    .usage-item:last-child { margin-bottom:0; }
    .usage-label-row { display:flex; align-items:center; gap:12px; }
    .usage-label { font-size:.875rem; font-weight:600; min-width:190px; flex-shrink:0; }
    .usage-bar-wrap { flex:1; background:var(--gray-200); border-radius:9999px; height:8px; overflow:hidden; }
    .usage-bar-fill { height:100%; border-radius:9999px; background:var(--charcoal); transition:width .4s ease; }
    .usage-bar-fill.warn { background:var(--amber); }
    .usage-bar-fill.over { background:var(--red); }
    .usage-count { font-size:.875rem; font-weight:600; flex-shrink:0; width:64px; text-align:right; color:var(--charcoal); }
    .usage-sub { font-size:.78rem; color:var(--gray-400); margin-top:4px; padding-left:202px; }
  </style>

######## LEGACY STYLE BLOCK: public/cert/cert-view.html ########
  <style>
    body { background: var(--gray-100); }
    .cert-page { min-height: 100vh; }
    .tier-banner { padding: 10px 24px; font-size: .8rem; font-weight: 600; text-align: center; letter-spacing: .03em; }
    .tier-banner--public    { background: var(--gray-200); color: var(--gray-600); }
    .tier-banner--agency    { background: #DCFCE7; color: #15803D; border-bottom: 1px solid #86EFAC; }
    .tier-banner--onrequest { background: #FFF7ED; color: #C2410C; border-bottom: 1px solid #FED7AA; }
  </style>

######## LEGACY STYLE BLOCK: public/owner/dossier.html ########
  <style>
    .preview-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.75); z-index:300; align-items:center; justify-content:center; padding:24px; }
    .preview-overlay.open { display:flex; }
    .preview-box { background:#fff; border-radius:var(--radius); width:100%; max-width:920px; display:flex; flex-direction:column; box-shadow:0 20px 60px rgba(0,0,0,.3); max-height:90vh; }
    .preview-header { display:flex; align-items:center; justify-content:space-between; padding:14px 20px; border-bottom:1px solid var(--gray-200); flex-shrink:0; gap:12px; }
    .preview-title { font-weight:600; font-size:.9375rem; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; min-width:0; }
    .preview-close { background:none; border:none; cursor:pointer; font-size:1.1rem; color:var(--gray-400); padding:4px 10px; border-radius:4px; line-height:1; flex-shrink:0; }
    .preview-close:hover { background:var(--gray-100); color:var(--charcoal); }
    .preview-body { flex:1; overflow:auto; min-height:0; background:var(--gray-100); }
    .preview-body iframe { width:100%; height:75vh; min-height:400px; border:none; display:block; }
    .preview-body img { max-width:100%; max-height:75vh; display:block; margin:auto; padding:16px; box-sizing:border-box; }
    .preview-loading { display:flex; align-items:center; justify-content:center; height:300px; font-size:.875rem; color:var(--gray-400); }
    .preview-err { display:flex; align-items:center; justify-content:center; height:200px; font-size:.875rem; color:var(--red); }
  </style>

######## LEGACY STYLE BLOCK: public/owner/settings.html ########
  <style>
    .tab-bar { display:flex; gap:0; border-bottom:2px solid var(--gray-200); margin-bottom:28px; }
    .tab-btn { padding:10px 22px; border:none; background:none; font-size:.9rem; font-weight:600; color:var(--gray-400); cursor:pointer; border-bottom:2px solid transparent; margin-bottom:-2px; transition:color .15s, border-color .15s; }
    .tab-btn.active { color:var(--charcoal); border-bottom-color:var(--charcoal); }

    .danger-zone { border-color: #FECACA !important; }
    .danger-zone legend { color: var(--red); }

    .payment-method-card { border:1px solid var(--gray-200); border-radius:var(--radius); padding:16px 20px; display:flex; align-items:center; gap:14px; margin-bottom:12px; }
    .payment-method-card.is-default { border-color:#86EFAC; background:#F0FDF4; }
    .payment-method-icon { font-size:1.5rem; flex-shrink:0; width:36px; text-align:center; }
    .payment-method-info { flex:1; min-width:0; }
    .payment-method-name { font-weight:600; font-size:.9rem; }
    .payment-method-sub { font-size:.8rem; color:var(--gray-400); margin-top:2px; }

    .invoice-row { display:flex; align-items:center; gap:12px; padding:12px 0; border-bottom:1px solid var(--gray-100); }
    .invoice-row:last-child { border-bottom:none; }

    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:500px; padding:32px; box-shadow:var(--shadow-lg); max-height:90vh; overflow-y:auto; }

    .phone-row { display:flex; gap:8px; }
    .phone-row select { width:110px; flex-shrink:0; }
    .phone-row input { flex:1; }

    .tax-id-list { display:flex; flex-direction:column; gap:8px; margin-bottom:8px; }
    .tax-id-row { display:flex; gap:8px; }
    .tax-id-row input { flex:1; }

    .plan-banner { display:flex; align-items:center; justify-content:space-between; gap:16px; padding:16px 20px; background:var(--gray-100); border-radius:var(--radius); margin-bottom:24px; flex-wrap:wrap; }
    .plan-name { font-weight:700; font-size:1rem; }
    .plan-meta { font-size:.8rem; color:var(--gray-400); margin-top:2px; }
    .usage-item { margin-bottom:20px; }
    .usage-item:last-child { margin-bottom:0; }
    .usage-label-row { display:flex; align-items:center; gap:12px; }
    .usage-label { font-size:.875rem; font-weight:600; min-width:190px; flex-shrink:0; }
    .usage-bar-wrap { flex:1; background:var(--gray-200); border-radius:9999px; height:8px; overflow:hidden; }
    .usage-bar-fill { height:100%; border-radius:9999px; background:var(--charcoal); transition:width .4s ease; }
    .usage-bar-fill.warn { background:var(--amber); }
    .usage-bar-fill.over { background:var(--red); }
    .usage-count { font-size:.875rem; font-weight:600; flex-shrink:0; width:64px; text-align:right; color:var(--charcoal); }
    .usage-sub { font-size:.78rem; color:var(--gray-400); margin-top:4px; padding-left:202px; }
  </style>

######## LEGACY STYLE BLOCK: public/tenant/certificate-new.html ########
<style>
.mode-card {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 16px;
  border: 2px solid var(--gray-200);
  border-radius: var(--radius);
  cursor: pointer;
  transition: border-color .15s, background .15s;
  background: white;
}
.mode-card:hover { border-color: var(--gray-400); }
.mode-card--selected { border-color: var(--charcoal); background: var(--gray-100); }
.mode-card__icon { font-size: 1.5rem; }
.mode-card__title { font-weight: 700; font-size: .95rem; }
.mode-card__desc { font-size: .78rem; color: var(--gray-500); line-height: 1.4; }
.mode-card__badge { font-size: .72rem; font-weight: 600; margin-top: 4px; }
.directed-type-btn {
  padding: 8px 16px; font-size: .875rem; font-weight: 600; font-family: var(--font);
  border: 1.5px solid var(--gray-200); border-radius: var(--radius);
  background: white; color: var(--gray-500); cursor: pointer;
  transition: border-color .15s, color .15s, background .15s;
}
.directed-type-btn:hover { border-color: var(--gray-400); color: var(--charcoal); }
.directed-type-btn--active { border-color: var(--charcoal); color: var(--charcoal); background: var(--gray-100); }
.agency-opt:hover { background: var(--gray-100); }
</style>

######## LEGACY STYLE BLOCK: public/tenant/certificates.html ########
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Inter', sans-serif; background: white; color: #111827; padding: 48px; max-width: 680px; margin: 0 auto; }
    .header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 32px; padding-bottom: 20px; border-bottom: 2px solid #111827; }
    .logo { display: flex; align-items: center; gap: 10px; font-weight: 900; font-size: 1.25rem; }
    .logo-mark { background: #111827; color: white; width: 36px; height: 36px; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-size: .875rem; font-weight: 900; }
    .verified { display: inline-flex; align-items: center; gap: 6px; background: #f0fdf4; color: #15803d; border: 1px solid #86efac; border-radius: 20px; padding: 5px 14px; font-size: .8rem; font-weight: 600; }
    .body { display: flex; gap: 40px; align-items: flex-start; margin-bottom: 32px; }
    .qr-wrap { flex-shrink: 0; border: 8px solid white; box-shadow: 0 0 0 1px #e5e7eb; display: inline-block; }
    .info { flex: 1; }
    .cert-code { font-family: monospace; font-size: 1.75rem; font-weight: 700; letter-spacing: .12em; margin-bottom: 6px; }
    .property { font-size: 1.1rem; font-weight: 700; margin-bottom: 4px; }
    .details { color: #6b7280; font-size: .875rem; margin-bottom: 16px; }
    .meta-row { display: flex; gap: 24px; margin-bottom: 16px; }
    .meta-box { }
    .meta-label { font-size: .7rem; text-transform: uppercase; letter-spacing: .06em; color: #9ca3af; margin-bottom: 3px; }
    .meta-val { font-size: .9375rem; font-weight: 600; }
    .status-badge { display: inline-block; padding: 4px 14px; border-radius: 20px; font-weight: 700; font-size: .875rem; color: white; background: ${certIsValid ? '#16a34a' : '#dc2626'}; }
    .elig-badge { display: inline-block; padding: 4px 14px; border-radius: 20px; font-weight: 600; font-size: .875rem; background: ${eligible ? '#f0fdf4' : '#fffbeb'}; color: ${eligible ? '#15803d' : '#b45309'}; border: 1px solid ${eligible ? '#86efac' : '#fcd34d'}; }
    .recipient { font-size: .875rem; color: #374151; margin-bottom: 16px; }
    .footer { border-top: 1px solid #e5e7eb; padding-top: 16px; font-size: .75rem; color: #9ca3af; display: flex; justify-content: space-between; }
    .scan-hint { font-size: .75rem; color: #9ca3af; margin-top: 6px; text-align: center; }
    @media print {
      body { padding: 24px; }
      @page { margin: 1cm; size: A4; }
    }
  </style>

######## LEGACY STYLE BLOCK: public/tenant/documents.html ########
  <style>
    .doc-card { border: 1px solid var(--gray-200); border-radius: var(--radius); overflow: hidden; margin-bottom: 16px; }
    .doc-card__head { padding: 14px 20px; display: flex; align-items: center; justify-content: space-between; background: var(--gray-100); border-bottom: 1px solid var(--gray-200); }
    .doc-card__title { font-weight: 600; font-size: .9375rem; display: flex; align-items: center; gap: 8px; }
    .doc-card__body { padding: 20px; }
    .doc-card--verified .doc-card__head { background: #F0FDF4; border-bottom-color: #86EFAC; }
    .doc-card--flagged .doc-card__head { background: #FFFBEB; border-bottom-color: #FCD34D; }
    .doc-card--rejected .doc-card__head { background: #FEF2F2; border-bottom-color: #FECACA; }
    .doc-card--processing .doc-card__head { background: #EFF6FF; border-bottom-color: #93C5FD; }
    .upload-btn-row { display: flex; gap: 8px; align-items: center; margin-top: 14px; }
    .file-info { display: flex; align-items: center; gap: 10px; padding: 10px 14px; background: var(--gray-100); border-radius: var(--radius); font-size: .875rem; }
    .file-info__name { font-weight: 500; flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .file-info__date { color: var(--gray-400); font-size: .8rem; flex-shrink: 0; }
    .ocr-grid { display: grid; grid-template-columns: 160px 1fr; gap: 4px 12px; margin-top: 10px; font-size: .8125rem; }
    .ocr-grid__key { color: var(--gray-400); }
    .ocr-grid__val { font-weight: 500; color: var(--charcoal); }
    .confidence-bar { height: 4px; background: var(--gray-200); border-radius: 2px; margin-top: 8px; overflow: hidden; }
    .confidence-bar__fill { height: 100%; border-radius: 2px; }
    .upload-zone { border: 2px dashed var(--gray-200); border-radius: var(--radius); padding: 24px; text-align: center; cursor: pointer; transition: border-color .15s; }
    .upload-zone:hover { border-color: var(--charcoal); }
    .upload-zone__icon { font-size: 1.5rem; margin-bottom: 8px; }
    .upload-zone__text { font-weight: 500; font-size: .875rem; margin-bottom: 4px; }
    .upload-zone__hint { font-size: .75rem; color: var(--gray-400); }
    .spinner { display: inline-block; width: 14px; height: 14px; border: 2px solid var(--gray-200); border-top-color: var(--charcoal); border-radius: 50%; animation: spin .6s linear infinite; vertical-align: middle; margin-right: 6px; }
    @keyframes spin { to { transform: rotate(360deg); } }
    .preview-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.75); z-index:300; align-items:center; justify-content:center; padding:24px; }
    .preview-overlay.open { display:flex; }
    .preview-box { background:#fff; border-radius:var(--radius); width:100%; max-width:920px; display:flex; flex-direction:column; box-shadow:0 20px 60px rgba(0,0,0,.3); max-height:90vh; }
    .preview-header { display:flex; align-items:center; justify-content:space-between; padding:14px 20px; border-bottom:1px solid var(--gray-200); flex-shrink:0; gap:12px; }
    .preview-title { font-weight:600; font-size:.9375rem; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; min-width:0; }
    .preview-close { background:none; border:none; cursor:pointer; font-size:1.1rem; color:var(--gray-400); padding:4px 10px; border-radius:4px; line-height:1; flex-shrink:0; }
    .preview-close:hover { background:var(--gray-100); color:var(--charcoal); }
    .preview-body { flex:1; overflow:auto; min-height:0; background:var(--gray-100); }
    .preview-body iframe { width:100%; height:75vh; min-height:400px; border:none; display:block; }
    .preview-body img { max-width:100%; max-height:75vh; display:block; margin:auto; padding:16px; box-sizing:border-box; }
    .preview-loading { display:flex; align-items:center; justify-content:center; height:300px; font-size:.875rem; color:var(--gray-400); }
    .preview-err { display:flex; align-items:center; justify-content:center; height:200px; font-size:.875rem; color:var(--red); }
  </style>

######## LEGACY STYLE BLOCK: public/tenant/settings.html ########
  <style>
    .tab-bar { display:flex; gap:0; border-bottom:2px solid var(--gray-200); margin-bottom:28px; }
    .tab-btn { padding:10px 22px; border:none; background:none; font-size:.9rem; font-weight:600; color:var(--gray-400); cursor:pointer; border-bottom:2px solid transparent; margin-bottom:-2px; transition:color .15s, border-color .15s; }
    .tab-btn.active { color:var(--charcoal); border-bottom-color:var(--charcoal); }

    .danger-zone { border-color: #FECACA !important; }
    .danger-zone legend { color: var(--red); }

    .payment-method-card { border:1px solid var(--gray-200); border-radius:var(--radius); padding:16px 20px; display:flex; align-items:center; gap:14px; margin-bottom:12px; }
    .payment-method-card.is-default { border-color:#86EFAC; background:#F0FDF4; }
    .payment-method-icon { font-size:1.5rem; flex-shrink:0; width:36px; text-align:center; }
    .payment-method-info { flex:1; min-width:0; }
    .payment-method-name { font-weight:600; font-size:.9rem; }
    .payment-method-sub { font-size:.8rem; color:var(--gray-400); margin-top:2px; }

    .invoice-row { display:flex; align-items:center; gap:12px; padding:12px 0; border-bottom:1px solid var(--gray-100); }
    .invoice-row:last-child { border-bottom:none; }

    .modal-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.45); z-index:200; align-items:center; justify-content:center; }
    .modal-overlay.open { display:flex; }
    .modal-box { background:white; border-radius:var(--radius); width:100%; max-width:500px; padding:32px; box-shadow:var(--shadow-lg); max-height:90vh; overflow-y:auto; }

    .phone-row { display:flex; gap:8px; }
    .phone-row select { width:110px; flex-shrink:0; }
    .phone-row input { flex:1; }

    .tax-id-list { display:flex; flex-direction:column; gap:8px; margin-bottom:8px; }
    .tax-id-row { display:flex; gap:8px; }
    .tax-id-row input { flex:1; }

    .plan-banner { display:flex; align-items:center; justify-content:space-between; gap:16px; padding:16px 20px; background:var(--gray-100); border-radius:var(--radius); margin-bottom:24px; flex-wrap:wrap; }
    .plan-name { font-weight:700; font-size:1rem; }
    .plan-meta { font-size:.8rem; color:var(--gray-400); margin-top:2px; }
    .usage-item { margin-bottom:20px; }
    .usage-item:last-child { margin-bottom:0; }
    .usage-label-row { display:flex; align-items:center; gap:12px; }
    .usage-label { font-size:.875rem; font-weight:600; min-width:190px; flex-shrink:0; }
    .usage-bar-wrap { flex:1; background:var(--gray-200); border-radius:9999px; height:8px; overflow:hidden; }
    .usage-bar-fill { height:100%; border-radius:9999px; background:var(--charcoal); transition:width .4s ease; }
    .usage-bar-fill.warn { background:var(--amber); }
    .usage-bar-fill.over { background:var(--red); }
    .usage-count { font-size:.875rem; font-weight:600; flex-shrink:0; width:64px; text-align:right; color:var(--charcoal); }
    .usage-sub { font-size:.78rem; color:var(--gray-400); margin-top:4px; padding-left:202px; }
  </style>

